import {Block, Text} from '@components';
import {COLORS} from '@theme';
import {View} from 'react-native';

export default function Receive() {
  return (
    <Block gap={14.8}>
      <Block backgroundColor={COLORS.white} padding={12} radius={8}></Block>
    </Block>
  );
}
